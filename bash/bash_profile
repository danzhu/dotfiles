#!/bin/bash

export DOT=$(dirname $(dirname $(readlink -f ~/.bash_profile)))

if [[ -f ~/.profile ]]; then
    source ~/.profile
fi

if [[ -f ~/.bashrc ]]; then
    source ~/.bashrc
fi

source $DOT/common/path
source $DOT/common/env
