#!/bin/bash

export DOT=$(dirname $(dirname $(readlink -f ~/.bash_profile)))

if [[ -f ~/.profile ]]; then
    source ~/.profile
fi

if [[ -f ~/.bashrc ]]; then
    source ~/.bashrc
fi

source "$DOT/shell/path"
source "$DOT/shell/env"
