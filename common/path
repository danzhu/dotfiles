# -*- mode: sh -*-

[[ -d "$HOME/.cargo/bin" ]] && PATH="$HOME/.cargo/bin:$PATH"
[[ -d "$HOME/.npm/bin" ]] && PATH="$HOME/.npm/bin:$PATH"
export PATH="$HOME/bin:$HOME/.local/bin:$PATH"

if [[ -f $DOT/common/path.local ]]; then
    source $DOT/common/path.local
fi

# vim: ft=sh
