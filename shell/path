# -*- mode: sh -*-

[[ -d "$HOME/.cargo/bin" ]] && PATH="$HOME/.cargo/bin:$PATH"
[[ -d "$HOME/.npm/bin" ]] && PATH="$HOME/.npm/bin:$PATH"
export PATH="$HOME/bin:$HOME/.local/bin:$DOT/bin:$PATH"

if [[ -f $DOT/shell/path.local ]]; then
    source $DOT/shell/path.local
fi

# vim: ft=sh
