# -*- mode: sh -*-

export DOT=$(dirname $(dirname $(readlink -f ~/.xprofile)))

if [[ -f ~/.profile ]]; then
    source ~/.profile
fi

source $DOT/shell/path
source $DOT/shell/env

setxkbmap -variant dvp -option ctrl:nocaps

# vim: ft=sh
