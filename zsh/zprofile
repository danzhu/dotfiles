# auto start X
if [[ -z "$DISPLAY" && -n "$XDG_VTNR" && "$XDG_VTNR" -eq 1 ]] &&
    (( $+commands[startx] )); then
    exec startx
fi

if [[ -f ~/.profile ]]; then
    source ~/.profile
fi

source ~/.dotfiles/common/path

# auto start tmux
if [[ -n "$SSH_CONNECTION" && -z "$TMUX" ]] && (( $+commands[tmux] )); then
    if tmux new -A -s develop; then
        exit
    fi
fi

# vim: ft=zsh
